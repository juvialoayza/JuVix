const router = require("express").Router();
const User = require("../models/User.model");

// para cuando hagamos el middleware 
//const {isLoggedIn, isAdmin} = require("../middleware/auth.routes.js");



module.exports = router;